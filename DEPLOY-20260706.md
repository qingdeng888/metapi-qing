# 快速部署指南 - 自动同步功能修复版

## 📦 拉取最新镜像

### AMD64 架构（x86_64）
```bash
docker pull qingdeng/metapi-qing:20260706
# 或
docker pull qingdeng/metapi-qing:latest
```

### ARM64 架构（Apple Silicon / ARM 服务器）
```bash
docker pull qingdeng/metapi-qing:20260706-arm64
```

## 🚀 启动容器

```bash
docker run -d \
  --name metapi \
  -p 4000:4000 \
  -v $(pwd)/data:/app/data \
  qingdeng/metapi-qing:20260706
```

## ✨ 新功能使用

### 启用自动同步

1. 访问管理面板 → 下游密钥
2. 创建或编辑密钥
3. 展开"高级配置"部分
4. **勾选"自动同步路由模型"**
5. 点击保存

### 效果

启用后，该密钥的模型列表将：
- ✅ 自动包含所有可用模型
- ✅ 自动跟随路由变化实时更新
- ✅ 创建/删除站点、账号、令牌时自动同步
- ✅ 手动重建路由时自动同步

### 验证

查看容器日志，应该看到类似输出：
```
[sync] Found 2 keys to sync
[sync] Found 9 enabled routes
[sync] Exact models: 9, Group routes: 0
[sync] Updated key 1 (密钥名称)
[sync] Synced 2 keys
```

## 🔄 更新现有部署

```bash
# 停止并删除旧容器
docker stop metapi && docker rm metapi

# 拉取新镜像
docker pull qingdeng/metapi-qing:20260706

# 启动新容器（保留原有数据）
docker run -d \
  --name metapi \
  -p 4000:4000 \
  -v $(pwd)/data:/app/data \
  qingdeng/metapi-qing:20260706
```

## 📝 更新内容

### 修复的问题
1. ✅ 勾选"自动同步"后再次编辑时勾选消失
2. ✅ 勾选"自动同步"但模型列表不更新
3. ✅ 创建新渠道后密钥不会自动获得新模型

### 技术细节
详见：[CHANGELOG-20260706.md](./CHANGELOG-20260706.md)

## ⚠️ 注意事项

- 启用自动同步会**覆盖**手动设置的模型列表
- 如需自定义特定模型，请不要启用自动同步
- 同步为异步后台操作，不影响主流程性能

## 🆘 问题排查

### 勾选后模型仍未同步

1. 查看容器日志：`docker logs metapi | grep sync`
2. 确认有可用的路由（至少一个站点+账号+令牌）
3. 手动触发：点击"重建路由"按钮

### 查看同步日志

```bash
docker logs -f metapi 2>&1 | grep "\[sync\]"
```

## 🔗 相关链接

- 代码仓库：https://github.com/qingdeng888/metapi-qing
- Docker Hub：https://hub.docker.com/r/qingdeng/metapi-qing
- 问题反馈：https://github.com/qingdeng888/metapi-qing/issues
