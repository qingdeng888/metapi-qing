# 模型自动启用逻辑修改说明

## 最新修复
**日期：2026-07-05**

### 修复的问题
之前的逻辑存在严重 Bug：首次添加渠道时，由于 `previousEnabledModels` 为空，导致所有新发现的模型都被设置为禁用状态，用户看不到任何可用模型。

### 修复方案
在 `shouldEnableModel` 函数中增加判断：
- **首次添加渠道**（`previousAllModels.length === 0`）：所有模型默认启用
- **后续刷新**：新发现的模型默认禁用，只有之前启用过的模型保持启用状态

### 修改代码位置
`src/server/services/modelService.ts` 第 ~645-720 行

---

## 修改日期
2026-07-04

## 问题描述
之前的逻辑是：只要上游渠道发现了新模型，就会自动启用并加入路由，导致路由表变得混乱。

## 修改内容

### 核心逻辑变更
在 `src/server/services/modelService.ts` 中的 `refreshModelsForAccount` 函数：

1. **新增逻辑：保存之前启用的模型列表**
   - 在清除现有模型之前，保存所有已启用的模型名称到 `previousEnabledModels` Set

2. **新增函数：`shouldEnableModel(modelName: string)`**
   - 判断新发现的模型是否应该启用
   - 只有在 `previousEnabledModels` 中存在的模型才返回 `true`
   - 新发现的模型默认返回 `false`（禁用状态）

3. **修改所有模型插入逻辑**
   - Codex OAuth 模型发现（行 ~725）
   - Claude OAuth 模型发现（行 ~811）
   - Gemini CLI OAuth 模型发现（行 ~911）
   - Antigravity OAuth 模型发现（行 ~997）
   - 标准平台模型发现（行 ~1263）

   所有地方的 `available: true` 都改为 `available: shouldEnableModel(modelName)`

## 行为变化

### 之前的行为（2026-07-04 初始版本 - 有 Bug）
❌ **首次添加渠道** → 所有模型默认禁用 → 用户看不到任何模型（Bug）
✅ 上游新增模型 → 默认禁用 → 不会自动加入路由
✅ 用户手动启用模型后 → 下次刷新时继续保持启用状态
✅ 上游删除模型 → 自动从数据库删除 → 自动从路由移除

### 修改后的行为（2026-07-05 修复版本）
✅ **首次添加渠道** → 所有模型默认启用 → 用户可以正常使用
❌ 上游新增模型 → 默认禁用 → 不会自动加入路由
✅ 用户手动启用模型后 → 下次刷新时继续保持启用状态
✅ 上游删除模型 → 自动从数据库删除 → 自动从路由移除

## 影响范围

### 不受影响的功能
- 手动添加的模型（`isManual: true`）不受影响
- 已经启用的模型在刷新后继续保持启用状态
- 模型删除逻辑保持不变

### 受影响的功能
- 首次添加渠道时，所有发现的模型默认为禁用状态
- 上游新增模型时，需要用户手动启用才会出现在路由中

## 用户使用流程

1. 添加新渠道 → 刷新模型
2. 所有发现的模型默认为**禁用**状态
3. 用户在模型管理页面手动启用需要的模型
4. 系统会记住启用状态
5. 下次刷新时，已启用的模型继续保持启用，新发现的模型默认禁用

## 代码位置
- 文件：`src/server/services/modelService.ts`
- 函数：`refreshModelsForAccount()`
- 修改行数：约 620-1270 行

## 测试建议

### 测试场景 1：新增渠道
1. 添加一个新的 New API 渠道
2. 刷新模型
3. 验证：所有模型应该处于禁用状态

### 测试场景 2：手动启用模型
1. 手动启用某个模型（如 `gpt-4`）
2. 再次刷新模型
3. 验证：`gpt-4` 仍然是启用状态

### 测试场景 3：上游新增模型
1. 上游新增一个模型（模拟：手动在数据库中删除某个模型，然后刷新）
2. 刷新模型
3. 验证：新增的模型应该是禁用状态

### 测试场景 4：上游删除模型
1. 上游删除某个已启用的模型
2. 刷新模型
3. 验证：该模型从数据库和路由中被删除

## 相关表结构
- `model_availability` - 存储账号级别的模型可用性
  - `available` - 布尔值，是否启用该模型
  - `isManual` - 布尔值，是否为手动添加
- `site_disabled_models` - 存储站点级别禁用的模型
- `token_routes` - 存储路由配置
- `route_channels` - 存储路由通道

## 向后兼容性
✅ 完全向后兼容
- 已有的启用模型不受影响
- 只影响新发现的模型的默认状态
